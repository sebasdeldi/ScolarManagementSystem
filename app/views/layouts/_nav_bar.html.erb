<% if current_user %>
  <% if current_page?(root_path) and !current_user.has_role? :admin %>
    <!-- Aquí va el navbar para cuando los estudiantes o profesores esten logueados y visitan el landing. pero al admin no le salga nada -->

    <!-- render "layouts/nav_bar_landing"  -->
  <% else %>
    <%= render "layouts/nav_bar_panel" %>
  <% end %>
<% end %>

<% if current_page?(root_path) and !current_user %>
  <!-- Aquí va el navbar del landing page cuando no hay nadie logueado-->
  <!--  render "layouts/nav_bar_landing"  -->
<% end %>



